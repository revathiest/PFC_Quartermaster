const { SlashCommandBuilder } = require('@discordjs/builders')module.exports ={	data: new SlashCommandBuilder()		.setName('help')		.setDescription('Sends Help information to the user'),	//============================================================================	// 20211109 krh Initial Coding	// 20211112 krh Moved function to it's own file	//============================================================================	async execute(interaction, client){				const { MessageEmbed } = require ('discord.js')		const bot = require ( './../bot.js')					const responseEmbed = new MessageEmbed()			.setColor('#0099ff')			.setTitle('PFC Quartermaster Help')			.addField('help', 'Returns this help information.')			.addField('user <username>', 'Returns information about that user from the RSI website.')			.addField('org <tag>', 'Returns information about an Organization from the RSI website.')			.addField('ship <shipname>', 'Returns information about a ship from the RSI website.  If multiple ships are found, only the first will be displayed.')			.addField('shipshop <tag (optional)>', 'Returns information about Ship Shops and their inventory.')			.addField('play <song>', 'Adds the song to the music queue.')			.addField('playlist <playlist>', 'Adds the playlist to the music queue.')			.addField('skip', 'Skips the song that is currently playing.')			.addField('stop', 'Stops the current song and destroys the queue.')			.addField('pause', 'Pauses the currently playing song.')			.addField('resume', 'Continues a song that has been paused.')			.addField('clear', 'Clears the current music queue.')			.addField('playing', 'Returns the currently playing song.')		await interaction.user.send({ embeds: [responseEmbed] })		await interaction.reply({content: 'Check your DMs', ephemeral: true})		}}